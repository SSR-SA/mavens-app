// import { TextInput } from "react-native";
import { SearchBoxContainer, SearchBox } from "./searchContainer.styles";

const SearchContainer = () => {
  return (
    <SearchBoxContainer>
      <SearchBox />
    </SearchBoxContainer>
  );
};

export default SearchContainer;
